<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moon.mapper.DishMapper">


    <insert id="addDish" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish
        (name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        VALUES
        (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>


    <update id="updateDish">
        UPDATE dish
        SET
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        WHERE id = #{id}
    </update>


    <delete id="deleteBatch">
        DELETE FROM dish
        WHERE id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>


    <select id="findPage" resultType="com.moon.vo.DishVO">
        SELECT
        dish.*,
        category.name categoryName
        FROM dish
        LEFT JOIN category ON dish.category_id = category.id
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="name != null">
                AND name LIKE '%${name}%'
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
        </where>
        ORDER BY dish.update_time DESC
    </select>
    <select id="getStatuses" resultType="java.lang.Integer">
        SELECT status FROM dish WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
    <select id="findDishList" resultType="com.moon.entity.Dish">
        SELECT * FROM dish
        <where>
            <if test="name != null">
                AND name LIKE '%${name}%'
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>
    <select id="getDishVOByCategoryId" resultType="com.moon.vo.DishVO">
        SELECT dish.*, category.name categoryName
        FROM dish LEFT JOIN category ON dish.category_id = category.id
        WHERE dish.category_id = #{categoryId}
        AND dish.status = 1
    </select>

</mapper>